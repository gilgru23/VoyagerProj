export class Dosing {
  constructor(id, podSerialNum, podType, amount, time) {
    this.id = id
    this.podSerialNum = podSerialNum
    this.podType = podType
    this.amount = amount
    this.time = time
  }
}
